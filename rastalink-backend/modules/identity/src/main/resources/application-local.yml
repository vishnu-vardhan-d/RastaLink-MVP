# ========================================
# LOCAL MACHINE DEVELOPMENT CONFIGURATION
# ========================================
# This file is for running on your local machine (after git clone)
# Never modify this for production use

server:
  port: 8080
  address: 127.0.0.1  # Bind only to localhost for security
  servlet:
    context-path: /api
  compression:
    enabled: false  # Disabled for faster local development

spring:
  # Local Development Database (H2 In-Memory)
  datasource:
    url: jdbc:h2:mem:rastalink_local;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
    driver-class-name: org.h2.Driver
    username: sa
    password: 
    hikari:
      maximum-pool-size: 5
      minimum-idle: 1
  
  h2:
    console:
      enabled: true
      path: /h2-console
      settings:
        web-allow-others: true  # Allow external connections for debugging
  
  jpa:
    hibernate:
      ddl-auto: create-drop  # Recreate schema on startup for local
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.H2Dialect
        generate_statistics: true
  
  # Local Development Security (Disabled for easy development)
  security:
    enabled: false

# Local Development Logging (Verbose)
logging:
  level:
    root: INFO
    com.rastalink: DEBUG
    org.springframework.security: DEBUG
    org.springframework.web: DEBUG
    org.hibernate.SQL: DEBUG
    org.hibernate.type.descriptor.sql.BasicBinder: TRACE
  pattern:
    console: "%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n"

# Local Development Management (Essential endpoints only)
management:
  server:
    address: 127.0.0.1  # Bind actuator to localhost only
  endpoints:
    web:
      exposure:
        include: "health,info"  # Only expose essential endpoints
      base-path: /actuator
  endpoint:
    health:
      show-details: always
    shutdown:
      enabled: true  # Allow graceful shutdown for local development